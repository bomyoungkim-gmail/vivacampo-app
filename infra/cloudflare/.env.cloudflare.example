# Cloudflare Environment Variables
# Copy to .env.cloudflare and fill in your values
# These are used by wrangler CLI, NOT by Docker Compose

# Required for wrangler deploy
CLOUDFLARE_API_TOKEN=your-api-token-here
CLOUDFLARE_ACCOUNT_ID=your-account-id-here

# How to get these values:
# 1. Login to Cloudflare Dashboard: https://dash.cloudflare.com
# 2. Account ID: visible in the URL or right sidebar of any zone
# 3. API Token: My Profile → API Tokens → Create Token
#    - Use template "Edit Cloudflare Workers"
#    - Or create custom with: Workers:Edit, Zone:Read

# Usage:
# source .env.cloudflare && wrangler publish --env production

# For CI/CD (GitHub Actions):
# Add these as repository secrets:
# - CLOUDFLARE_API_TOKEN
# - CLOUDFLARE_ACCOUNT_ID
