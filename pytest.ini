[pytest]
asyncio_mode = auto
markers =
    unit: fast tests without external dependencies
    integration: tests that require external services or full app stack
    e2e: end-to-end user flow tests
    contract: port/adapter contract tests
    security: multi-tenant and access control tests
